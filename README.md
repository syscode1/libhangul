# libhangul for Visual Studio 2022

libhangul 프로젝트를 Visual Studio 2022 에서 Compile (DLL) 하는 Project 입니다.

Build event로 마지막에 vc_redist.x64.exe를 다운로드 합니다.

## 설치 및 초기 설정

git clone git@github.com:syscode1/libhangul.git

git submodule init

git submodule update
